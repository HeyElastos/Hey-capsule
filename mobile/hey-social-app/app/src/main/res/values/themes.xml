<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose drives the real theming; this just gives the Activity a dark,
         action-bar-less Material shell before Compose takes over. -->
    <style name="Theme.HeySocial" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:statusBarColor">#091427</item>
        <item name="android:navigationBarColor">#091427</item>
        <item name="android:windowBackground">#091427</item>
    </style>
</resources>
