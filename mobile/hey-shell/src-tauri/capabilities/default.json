{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Hey launcher window. Custom commands (connect/load_config) are not gated here; only core defaults are needed.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
