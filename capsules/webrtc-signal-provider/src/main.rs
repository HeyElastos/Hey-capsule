//! webrtc-signal-provider — relays SDP offer/answer and ICE candidates
//! over Carrier gossip topics. Media itself is WebRTC P2P in the browser.
//!
//! Phase 0: stub.

fn main() {
    eprintln!("webrtc-signal-provider stub — Phase 0 scaffold, not yet implemented");
    std::process::exit(0);
}
